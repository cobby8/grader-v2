# grader-v2 engine 의존성 (Python 3.11)
# CMYK 무손실 PDF 합성 핵심
pikepdf==10.8.0
# 미리보기 PNG 렌더 전용 (출력 색 검증에는 사용 안 함)
PyMuPDF==1.27.2.3
# A-4 배번·이름 벡터 렌더: 글리프 → 윤곽 좌표 아웃라인화 (폰트 임베드 아님)
fonttools==4.63.0
# A-5 주문서 파싱: xlsx 읽기 (사이즈/수량/이름/배번 추출)
openpyxl==3.1.5

# webapp: FastAPI 백엔드(정적 화면 서빙 + /api). 엔진은 import만, 빌드 0.
fastapi==0.138.0
uvicorn==0.49.0
# 디자인/주문서 파일 업로드(multipart 폼) — 후속 단계 엔드포인트용
python-multipart==0.0.32
# 배포 인증(Supabase Auth): 토큰 introspection 검증용 HTTP 호출
#   GET {SUPABASE_URL}/auth/v1/user 로 토큰 주인을 물어본다(GRADER_REQUIRE_AUTH 켜질 때만 사용).
httpx==0.28.1

# 선택: Ghostscript(gswin64c) 설치 시 inkcov 색 교차검증 가능 (없어도 동작)
# (pip 패키지가 아니라 별도 설치 프로그램이라 위 목록에 넣지 않음)
